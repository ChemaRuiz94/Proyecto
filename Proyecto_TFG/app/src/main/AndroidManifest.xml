<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chema.ptoyecto_tfg">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ptoyecto_TFG">
        <activity
            android:name=".activities.AdminActivity"
            android:exported="true"
            android:label="@string/title_activity_admin" />
        <activity
            android:name=".activities.SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".rv.AdapterRvEtiquetas"
            android:exported="true" />
        <activity
            android:name=".activities.DetailActivity"
            android:exported="true" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".activities.SearchResultMapsActivity"
            android:exported="true"
            android:label="@string/title_activity_search_result_maps" />
        <activity
            android:name=".activities.ChatActivity"
            android:exported="true" />
        <activity
            android:name=".activities.ArtistMuroConatinerActivity"
            android:exported="true" />
        <activity
            android:name=".activities.ListResutlActivity"
            android:exported="true" />
        <activity
            android:name=".TabBasicUserActivity"
            android:exported="true"
            android:label="@string/title_activity_tab_basic_user"
            android:theme="@style/Theme.Ptoyecto_TFG.NoActionBar" />
        <activity
            android:name=".activities.SelectStudioMapsActivity"
            android:exported="true"
            android:label="@string/title_activity_select_studio_maps"
            android:theme="@style/Theme.Ptoyecto_TFG.ActionBar" />
        <activity
            android:name=".navigation.basic.BasicUserNavDrawActivity"
            android:exported="true"
            android:label="@string/title_activity_basic_user_nav_draw"
            android:theme="@style/Theme.Ptoyecto_TFG.NoActionBar" />
        <activity
            android:name=".activities.SignUpArtistActivity"
            android:exported="true" />
        <activity
            android:name=".activities.SignUpBasicActivity"
            android:exported="true" />
        <activity
            android:name=".activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true" />
    </application>

</manifest>